// Fetch menu items via NoCodeBackend (placeholder)
import { useState } from "react";

export function useMenuData() {
  const [items, setItems] = useState([]);
  // Placeholder: fetch menu data
  return { items, setItems };
}
